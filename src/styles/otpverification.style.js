import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    inputsContainer: {
        display: "flex",
        flexDirection: "row",
        justifyContent: "space-evenly"
    },
    textInputs: {
        margin: 10,
        width: 60
    },
    logo: {
        resizeMode: "contain",
        height: 120,
        width: 120
    }
});